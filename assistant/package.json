{
  "name": "@converseai/assistant",
  "version": "1.0.0",
  "type": "module",
  "description": "Website Brain V2 — self-hosted grounded RAG assistant (no external LLM). Shared by the text endpoint and the realtime voice gateway.",
  "main": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "ingest": "tsx src/ingest/cli.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "@xenova/transformers": "^2.17.2"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4"
  }
}
